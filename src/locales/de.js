import antdLocale from 'antd/lib/locale-provider/de_DE';
import localeData from 'react-intl/locale-data/de';
import messages from './messages/de';

const config = {
    messages,
    antdLocale,
    locale: 'de',
    localeData
};

export default config;
